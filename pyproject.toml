[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "contracts-microservice"
version = "0.1.3"
description = "Shared contracts for microservices"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["contracts*"]
